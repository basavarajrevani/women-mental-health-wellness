import React from 'react';

// SmartNotifications component is completely disabled
// No context usage, no automatic notifications, no triggers
const SmartNotifications: React.FC = () => {
  console.log('🔕 SmartNotifications: Component disabled - no automatic notifications');
  return null;
};

export default SmartNotifications;